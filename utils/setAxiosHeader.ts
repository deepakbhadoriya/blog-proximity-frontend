import axios from "axios";

const setAxiosHeader = (token: string | boolean) => {
  if (token) {
    axios.defaults.headers.common["x-auth-token"] = token;
  } else {
    delete axios.defaults.headers.common["x-auth-token"];
  }
};

export default setAxiosHeader;
